{% extends 'base.html.twig' %}

{% block content %}

        <div class="content">
            <a href="{{path('author', {'id': author.id}) }}" >{{ author.name }}</a>


        </div>


{% endblock %}